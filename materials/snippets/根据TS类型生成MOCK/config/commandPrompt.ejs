<%- rawClipboardText %> 
使用 mock.js 生成跟上面 ts类型 一样字段的数据，所有字段都是用 mock.js 生成的，如果有数组则生成10个元素，
下面是 koa.js 的路由定义，请求这个路由的时候返回上面的随机数据，生成随机数据的逻辑放到路由内部
.get(`/api/mock`, async (ctx, next) => { 
	ctx.body = 
})
返回的路由定义的 markdown 代码块，只需要路由定义的代码，既 .get 开头的代码，router 也去掉，而且不需要 require 引入赖，外部已经引入

